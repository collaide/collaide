# -*- encoding : utf-8 -*-
require 'rspec'

describe 'Group::Status' do
  describe 'post a new status'
  before(:each) do
    sign_in_as_a_valid_user
  end
  it 'visit the index page with the form' do
    visit ''
  end
end
