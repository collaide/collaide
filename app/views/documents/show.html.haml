%h3= @document.title
%p= @document.description
%p
  Télécharger:
  =link_to @document.files.first.file.name, @document.files.first.file.url
=link_to t('documents.show.back'), documents_path, class: 'button'
=link_to t("documents.show.modify"), edit_document_path, class: 'button'
