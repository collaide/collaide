<%= simple_form_for(@study) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :started_at %>
    <%= f.input :ended_at %>
    <%= f.input :orientatoin %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
