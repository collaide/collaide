-#content_for :title, t('users.show.meta_title', user: @user.name_to_show)
-content_for :title, t('group.members.title_page', group: @work_group.name)

-#=render(:layout => 'layout') do
-#  %h1= t('group.members.title_page', group: @work_group.name)
-#  -if @work_group.members.empty?
-#    =t'group.members.empty'
-#  -else
-#    %ul.large-block-grid-4.medium-block-grid-3.small-block-grid-2#users
-#      =render @work_group.u_members.to_a
-#    #paginate.hide
-#      =paginate @work_group.u_members.to_a

=render(:layout => 'layout') do
  %h1= t('group.members.title_page', group: @work_group.name)
  -if @work_group.members.empty?
    =t'group.members.empty'
  -else
    %ul
      -@work_group.members.each do |m|
        %li
          -gm = m.group_members.where(group: @work_group).take
          =link_to m.to_s, m
          ="occupe le rôle de #{gm.role.text} et a rejoint le groupe #{gm.joined_method.text}"
          -if gm.invited_or_added_by
            (par :
            =link_to gm.invited_or_added_by.to_s, gm.invited_or_added_by
            )
