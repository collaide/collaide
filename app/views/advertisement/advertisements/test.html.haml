=@salut
-if @books.first
  %ol
    -@books.each do |b|
      %li= image_tag b.image_link(), width: 100, height: 100
      #{b.title} de #{b.authors} (ISBN : #{b.isbn}). Note : #{b.average_rating}....... DESCRIPTION: #{b.description}