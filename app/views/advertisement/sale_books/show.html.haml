.row
  .large-4.columns
    =image_tag @sale_book.book.image_link
  .large-8.columns
    %h1= @sale_book.book.title
    %h2= "De #{@sale_book.book.authors}"
    %p
      Edité
      -if @sale_book.book.publisher
        =" par #{@sale_book.book.publisher}"
      -if @sale_book.book.published_date
        =" en #{@sale_book.book.published_date}"
    -if @sale_book.book.description
      %p= @sale_book.book.description





= link_to 'Edit', edit_advertisement_sale_book_path(@sale_book)
|
= link_to 'Back', advertisement_sale_books_path
=@google_book.inspect

:javascript
  $(document).ready(function() {
    alert("I'm loaded!");
  });
