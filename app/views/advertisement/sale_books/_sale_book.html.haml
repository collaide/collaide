-#.panel#sale_book_index
.row#sale_book_index
  .large-2.columns.small-12
    .row
      .small-12.columns
        %a.th{href: advertisement_sale_book_path(sale_book)}
          =book_image(sale_book.book)
    .row
      .small-12.columns
        %p= rating_for sale_book.book, 'note', star: 5
    -#.row
    -#  .small-12.columns
    -#    %p
    -#      =raw("<span>#{t('advertisements.index.hits')}</span>")
    -#      =sale_book.hits
    #star
  .small-12.columns.large-10
    %h5= link_to sale_book_title(sale_book), sale_book
    %h6.subheader= t('sale_books.index.subtitle1', book:sale_book.book.title)
    %h6.subheader
      =link_to(sale_book.user.name_to_show, sale_book.user)
      =t('sale_books.index.subtitle2', price: number_to_currency(sale_book.price, :unit => sale_book.currency.text))
    -if sale_book.description
      %p= sale_book.description.truncate(100, separator: ' ', omission: ' ...')
    -elsif sale_book.book.description
      %p= sale_book.book.description.truncate(100, separator: ' ', omission: ' ...')
    -else
      %p= t('sale_books.index.no_description')
    =show_link sale_book, t('sale_books.index.buy'), 'secondary'
    =show_link sale_book, t('advertisements.index.show'), 'secondary'
  %hr