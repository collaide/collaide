<h1>Listing user_group_users</h1>

<table>
  <tr>
    <th>Is admin</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @user_group_users.each do |user_group_user| %>
  <tr>
    <td><%= user_group_user.is_admin %></td>
    <td><%= link_to 'Show', user_group_user %></td>
    <td><%= link_to 'Edit', edit_user_group_user_path(user_group_user) %></td>
    <td><%= link_to 'Destroy', user_group_user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_group_user_path %>
