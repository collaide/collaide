%h3= @document.title
%p= @document.description
%p
  Télécharger:
  =link_to @document.files.first.file.name, @document.files.first.file.url unless @document.files.empty?
=link_to t('documents.show.back'), document_documents_path, class: 'button'
=edit_link @document, t('documents.show.modify')
=destroy_link @document, t('documents.show.delete')
